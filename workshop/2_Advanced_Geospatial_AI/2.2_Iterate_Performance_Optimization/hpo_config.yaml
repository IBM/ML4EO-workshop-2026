metrics:
  - val_loss  # Metric to optimize (minimize validation loss)

static:
  config: config_salt_marsh.yaml  # Base terratorch configuration
  epochs: 2               # Number of epochs per trial

hpo:  
  # Learning rate (continuous, log normal sampling)
  lr:
    type: float
    low: 1.0e-6
    high: 1.0e-3
    log: true